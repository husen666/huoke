# 🔥 火客智能营销获客系统 (HuoKeAgent)

AI 驱动的全渠道智能营销获客平台，整合中国主流互联网平台获客渠道，自动化完成线索获取、客户管理、智能客服、精准营销全链路闭环。

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Next.js 15 + React 19 + Tailwind CSS 4 + Recharts |
| 后端 | Hono + Node.js 22 + TypeScript |
| 数据库 | PostgreSQL 16 + Drizzle ORM |
| 向量库 | Qdrant |
| 缓存 | Redis 7 |
| 搜索 | Meilisearch |
| 文件存储 | MinIO |
| AI | DeepSeek (V3 + R1) |
| 部署 | Docker Compose |

## 快速开始

### 前置要求

- Node.js 22+
- pnpm 9+
- Docker Desktop

### 1. 克隆 & 安装

```bash
git clone <repo-url> huokeagent
cd huokeagent
cp .env.example .env
# 编辑 .env 中的 DEEPSEEK_API_KEY 等配置
pnpm install
```

### 2. 启动基础服务

```bash
# 启动 PostgreSQL、Redis、Qdrant、Meilisearch、MinIO
pnpm docker:dev
```

> 如遇 Docker Hub 拉取失败，用镜像代理：
> ```bash
> docker pull docker.1ms.run/postgres:16-alpine
> docker tag docker.1ms.run/postgres:16-alpine postgres:16-alpine
> # 对其他镜像同理
> ```

### 3. 初始化数据库

```bash
cd apps/server
npx drizzle-kit generate   # 生成迁移文件
npx drizzle-kit migrate    # 执行迁移
npx tsx src/db/seed.ts      # 写入种子数据
```

### 4. 启动开发服务

```bash
# 从项目根目录
pnpm dev
# 或分别启动：
# 后端: cd apps/server && pnpm dev  → http://localhost:4000
# 前端: cd apps/web && pnpm dev     → http://localhost:3000
```

### 5. 访问系统

打开浏览器访问 http://localhost:3000

默认账号：`admin@huoke.com` / `admin123`

## 生产部署

### Docker Compose 一键部署

```bash
cp .env.example .env
# 修改 .env 中的生产配置（数据库密码、JWT密钥、DeepSeek API Key 等）
docker compose up -d
```

系统将在 80 端口启动（通过 Nginx 反向代理）。

### 服务器要求

| 配置项 | 最低 | 推荐 |
|--------|------|------|
| CPU | 4 核 | 8 核+ |
| 内存 | 8 GB | 16 GB+ |
| 硬盘 | 50 GB SSD | 200 GB SSD |
| 系统 | Linux (Ubuntu 22.04) | Ubuntu 22.04 LTS |

## 项目结构

```
huokeagent/
├── apps/
│   ├── web/                    # Next.js 前端 (端口 3000)
│   │   ├── app/
│   │   │   ├── (auth)/login/   # 登录页
│   │   │   └── dashboard/      # 主面板
│   │   │       ├── leads/      # 线索管理
│   │   │       ├── customers/  # 客户管理
│   │   │       ├── inbox/      # 消息中心
│   │   │       ├── knowledge/  # 知识库
│   │   │       ├── campaigns/  # 营销活动
│   │   │       ├── workflows/  # 工作流
│   │   │       ├── analytics/  # 数据分析
│   │   │       └── settings/   # 系统设置
│   │   └── components/ui/      # UI 组件库
│   │
│   └── server/                 # Hono 后端 (端口 4000)
│       └── src/
│           ├── routes/         # API 路由 (8个模块)
│           ├── db/schema/      # 数据库表 (20张表)
│           ├── ai/             # DeepSeek AI 集成
│           ├── websocket/      # Socket.IO 实时通信
│           └── middleware/     # 认证 & 错误处理
│
├── packages/shared/            # 共享类型 & 常量
├── docker/                     # Docker 配置
├── docs/                       # 需求文档 & 设计规格
└── docker-compose.yml          # 生产部署配置
```

## API 端点

| 模块 | 路径 | 说明 |
|------|------|------|
| 认证 | `/api/v1/auth/*` | 登录/注册/刷新 Token |
| 线索 | `/api/v1/leads/*` | 线索 CRUD + 评分 + 分配 |
| 客户 | `/api/v1/customers/*` | 客户 CRUD + 标签 + 时间线 |
| 会话 | `/api/v1/conversations/*` | 消息 + AI回复 + 客服分配 |
| 知识库 | `/api/v1/knowledge-bases/*` | 文档管理 + RAG 查询 |
| 分析 | `/api/v1/analytics/*` | 数据统计 + 报表 |
| 营销 | `/api/v1/campaigns/*` | 营销活动管理 |
| 工作流 | `/api/v1/workflows/*` | 自动化工作流 |

## 功能模块

- **获客渠道中心** - 微信/抖音/小红书/百度等 12+ 平台接入
- **CRM 客户管理** - 360° 客户画像、线索评分、生命周期管理
- **智能客服** - 统一收件箱、AI 自动回复、人机协作
- **知识库 & AI 记忆** - RAG 检索增强、三层记忆系统
- **营销自动化** - 企微/钉钉触达、可视化工作流、A/B 测试
- **数据分析** - 渠道 ROI、转化漏斗、AI 智能洞察
